<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>BeastScan Voting Widget</title>
    <style>
        .cards-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 1rem;
            padding: 1rem;
        }
        .card {
            background: white;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            padding: 1rem;
            transition: 0.3s ease;
        }
        .card-img {
            width: 100%;
            max-height: 160px;
            object-fit: cover;
            border-radius: 8px;
        }
        .vote-buttons {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin: 0.5rem 0;
        }
        .btn-label {
            display: inline-block;
            padding: 0.5rem 1rem;
            background: #3490dc;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }
    </style>
</head>
<body>
<h1>🗳️ BeastScan Voting Widget</h1>

<div id="cards-container" class="cards-grid">
    <p id="loading-message">Loading cards...</p>
</div>

<script src="public/js/voting.js" defer></script>
</body>
</html>
